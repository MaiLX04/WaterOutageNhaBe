name: Notify via Telegram

on:
  push:
    branches:
      - main        # run on pushes to main
  schedule:
    - cron: "0 */1 * * *"   # every 1 hour
  workflow_dispatch:        # manual trigger

jobs:
  notify:
    runs-on: ubuntu-latest
    permissions:
      contents: write       # needed to push the updated sent.json back
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install

      - name: Run script (with retry)
        uses: nick-fields/retry@v3
        with:
          max_attempts: 3
          retry_wait_seconds: 120   # wait 2 minutes between retries
          timeout_minutes: 5
          command: npm start
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          URL: ${{ secrets.URL }}
          KEYWORDS: ${{ secrets.KEYWORDS }}
          CHECK_WINDOW_HOURS: ${{ vars.CHECK_WINDOW_HOURS }}

      - name: Commit updated sent.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cache/sent.json
          git diff --cached --quiet || git commit -m "chore: update sent cache [skip ci]"
          git push
